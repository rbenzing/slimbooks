// Theme and UI styling related types

// Status type derived from the statusColors object in themeUtils
export type StatusType = 
  | 'draft' 
  | 'pending' 
  | 'sent' 
  | 'paid' 
  | 'cancelled' 
  | 'overdue';